export interface Product {
  productId: number,
  userId: number,
  productName: string,
  verifiedDate: string,
  productImage: string,  
  subCategoryId: number
  summary: string,
  productDetails: string,
  productContact: string,
  subCategory: JSON
}
